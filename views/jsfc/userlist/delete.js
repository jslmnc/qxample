function deleteuser(id){ $(document).ready(function(){ Swal.fire({ text: "Are you sure you want to delete user?", icon: "warning", showCancelButton: true, buttonsStyling: false, confirmButtonText: "Yes, delete!", cancelButtonText: "No, cancel", customClass: { confirmButton: "btn fw-bold btn-danger", cancelButton: "btn fw-bold btn-active-light-primary" } }).then(function (result) { if (result.value) { $.ajax({ url:'_crud.php', type:'POST', data:{ id:id, action:'deleteuser' }, success:function(response){ Swal.fire({ text: "Successfully deleted!", icon: "success", buttonsStyling: false, confirmButtonText: "Ok, got it!", customClass: { confirmButton: "btn fw-bold btn-primary", } }).then(function(){ if(response==1){ window.location.href = 'users'; } else{ } window.location.href = 'users'; }); } }); } else if (result.dismiss === 'cancel') { Swal.fire({ text: "User was not deleted.", icon: "error", buttonsStyling: false, confirmButtonText: "Ok, got it!", customClass: { confirmButton: "btn fw-bold btn-primary", } }); } }); }); }