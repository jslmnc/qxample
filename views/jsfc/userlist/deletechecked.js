$(document).ready(function(){ $('#all_delete').click(function(){ Swal.fire({ text: "Are you sure you want to delete selected user?", icon: "warning", showCancelButton: true, buttonsStyling: false, confirmButtonText: "Yes, delete!", cancelButtonText: "No, cancel", customClass: { confirmButton: "btn fw-bold btn-danger", cancelButton: "btn fw-bold btn-active-light-primary" } }).then(function (result) { if (result.value) { var id=[]; $('#ucheckbox:checked').each(function(i){ id.push($(this).data('u-id')); }); $.ajax({ url:'_crud.php', type:'POST', data:{ id:id, action:'deleteall' }, success:function(response){ if(response==1){ Swal.fire({ text: "Successfully deleted!", icon: "success", buttonsStyling: false, confirmButtonText: "Ok, got it!", customClass: { confirmButton: "btn fw-bold btn-primary", } }).then(function(){ window.location.href = 'users'; }); } else{ } } }); } else if (result.dismiss === 'cancel') { Swal.fire({ text: "User was not deleted.", icon: "error", buttonsStyling: false, confirmButtonText: "Ok, got it!", customClass: { confirmButton: "btn fw-bold btn-primary", } }); } }); }); });